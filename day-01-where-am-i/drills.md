_*Open a terminal, print your working directory, then navigate to /var/log using an absolute path,
back to your home directory using ~, and finally return to /var/log using cd -. List its contents in
long, human-readable format.*_

1. pwdcd -
2. cd /var/log
3. cd ~
4. cd -
5. ls -al
